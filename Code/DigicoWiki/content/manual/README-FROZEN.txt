2026-09-15: content/manual and content/docs were generated by tools/extract_manual.py and then hand-edited
by the review pass (artifact cleanup, tables, captions). Do NOT re-run the extractor over these folders —
it would overwrite the edits. Re-extract into a scratch folder if a new manual issue ever needs importing.
